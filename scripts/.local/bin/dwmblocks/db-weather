#!/bin/sh

# ============================
# wttr.in Current Temperature
# ============================


case $BLOCK_BUTTON in
  1) setsid -f $TERMINAL -e "curl wttr.in/mingora";;
esac

temp=$(curl -s "wttr.in/mingora?0?T" | grep -i "°C" | grep -o [[:digit:]]* | head -n1)

echo 🔥"$temp"°
